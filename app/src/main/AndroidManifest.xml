<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mysoap">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BankFrom" />
        <activity android:name=".Updateaddress" />
        <activity android:name=".EditaddressActivity" />
        <activity android:name=".AddAddress" />
        <activity android:name=".ViewAddressActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".NewAddressActivity" />
        <activity android:name=".BankActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ContactActivity"
            android:label="@string/title_activity_contact" />
        <activity android:name=".ProducteightActivity" />
        <activity android:name=".ProductsevenActivity" />
        <activity android:name=".ProductsixActivity" />
        <activity android:name=".ProductfiveActivity" />
        <activity android:name=".ProductfourActivity" />
        <activity android:name=".ProductthreeActivity" />
        <activity android:name=".ProducttwoActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".UploadActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".ProductActivity" />
        <activity android:name=".AdminCategoryActivity" />
        <activity android:name=".AdminAddNewProductActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>